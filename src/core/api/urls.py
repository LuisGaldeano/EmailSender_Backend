from core.api import views as core_views
from django.urls import path

app_name = "core"

urlpatterns = [
    # path("send_msg/", core_views, name="send_message"),
    # path("check_msg/", core_views, name="check_message")
]